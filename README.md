# Packet-Tracer-NAT-PAT
Implementation of Port Address Translation (PAT) on a Cisco Router using Packet Tracer to enable multiple internal hosts to access an external network through a single public IP address.
